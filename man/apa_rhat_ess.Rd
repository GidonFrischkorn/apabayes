% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_rhat_ess}
\alias{apa_rhat_ess}
\title{Format convergence diagnostics}
\usage{
apa_rhat_ess(
  rhat = NULL,
  ess_bulk = NULL,
  ess_tail = NULL,
  digits = 2,
  markup = NULL
)
}
\arguments{
\item{rhat, ess_bulk, ess_tail}{Numeric vectors or \code{NULL}. Vectors must
share one length, or have length 1. At least one must be given.}

\item{digits}{Decimals for R-hat (effective sample sizes print as
integers with a thousands separator).}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}
}
\value{
A character vector of the common length; an \code{NA} in any given
part gives \code{NA_character_} for that element.
}
\description{
\verb{*R̂* = 1.00, bulk ESS = 1,240, tail ESS = 980} for one parameter, or
for the extremes over a fit. Parts that are \code{NULL} are omitted; the
order is always R-hat, bulk ESS, tail ESS.
}
\examples{
apa_rhat_ess(1.003, 1240, 980)
apa_rhat_ess(1.003, digits = 3, markup = "plain")
apa_rhat_ess(ess_bulk = c(1240, 400))
}
