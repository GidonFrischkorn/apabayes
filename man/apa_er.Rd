% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_er}
\alias{apa_er}
\title{Format evidence ratios}
\usage{
apa_er(x, digits = 2, markup = NULL, symbol = FALSE)
}
\arguments{
\item{x}{Numeric vector of evidence ratios, 0 or more; \code{NA} and \code{Inf}
allowed.}

\item{digits}{Decimals below 10.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}

\item{symbol}{\code{TRUE} prepends \verb{ER = } (or \verb{ER ≥ } at the bound).}
}
\value{
A character vector of \code{length(x)}; \code{NA} in gives
\code{NA_character_} out.
}
\description{
Evidence ratios from \code{brms::hypothesis()} (\code{Evid.Ratio}): a
Savage–Dickey density ratio for a point hypothesis, posterior odds for
a directional one. Printed as \verb{≥ 10,000} from 10,000 up (an infinite
ratio means no posterior draw contradicted the hypothesis), rounded to
an integer with a thousands separator from 1,000, to one decimal from
10, and to \code{digits} decimals below.
}
\details{
Small ratios print \code{0.00}; apabayes keeps the ratio as brms reports
it and, for point hypotheses, reports \code{1 / Evid.Ratio} through
\code{\link[=apa_bf]{apa_bf()}} as the Bayes factor against equality.
}
\examples{
apa_er(c(0.5, 5.3412, 23.456, 2345.6, 12345, Inf, NA))
apa_er(12345, symbol = TRUE)
}
