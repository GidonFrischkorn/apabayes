% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-blavaan.R, R/extract-lavaan.R
\name{apa_tidy_sem_fit.blavaan}
\alias{apa_tidy_sem_fit.blavaan}
\alias{apa_tidy_sem_fit}
\alias{apa_tidy_sem_fit.lavaan}
\title{Fit indices of a structural equation model}
\usage{
\method{apa_tidy_sem_fit}{blavaan}(
  x,
  model = NA_character_,
  pD = c("loo", "waic", "dic"),
  rescale = c("devM", "ppmc", "mcmc"),
  fit_ci_level = 0.9,
  ...
)

apa_tidy_sem_fit(x, ...)

\method{apa_tidy_sem_fit}{lavaan}(
  x,
  model = NA_character_,
  test = c("standard", "scaled", "robust"),
  rmsea_level = 0.9,
  ...
)
}
\arguments{
\item{x}{A fitted model.}

\item{model}{A single string naming the model in the \code{model} column,
or \code{NA} (the default).}

\item{pD}{Which effective-number-of-parameters estimator
\code{\link[blavaan:blavFitIndices]{blavaan::blavFitIndices()}} rescales the posterior chi-square with:
\code{"loo"} (its own default), \code{"waic"} or \code{"dic"}. Spelled as blavaan
spells it, rather than lower-cased, so that it is not read as the
\code{pd} of the parameters contract, which is the probability of
direction. Recorded in the \code{pD} attribute.}

\item{rescale}{How the posterior chi-square is rescaled: \code{"devM"} (the
default), \code{"ppmc"} or \code{"mcmc"}. Recorded in the \code{rescale} attribute.}

\item{fit_ci_level}{Mass of the fit indices' credible interval,
separate from the \code{ci_level} of the parameter table; blavaan's own
default is \code{0.90}.}

\item{...}{Passed to the method.}

\item{test}{Which chi-square family the indices come from:
\code{"standard"} (every fit has it), \code{"scaled"} (the \code{.scaled} variants
of a fit with a scaled test statistic, e.g. \code{estimator = "MLR"};
SRMR has none), or \code{"robust"} (the scaled chi-square with lavaan's
\code{.robust} CFI, TLI and RMSEA). A variant the fit does not carry is
an error.}

\item{rmsea_level}{Confidence level of the RMSEA interval; lavaan's
default is \code{0.90}.}
}
\value{
An \link{apabayes_tidy} tibble of type \code{"sem_fit"} with one row and
columns \code{model}, \code{chisq}, \code{df}, \code{p}, \code{cfi}, \code{tli}, \code{rmsea},
\code{rmsea_low}, \code{rmsea_high}, \code{rmsea_level}, \code{srmr}, \code{ppp}, \code{brmsea},
\code{brmsea_low}, \code{brmsea_high}, \code{bgammahat}, \code{bgammahat_low} and
\code{bgammahat_high}; the ones the fit does not carry are \code{NA}.
Attributes \code{estimator} and \code{n} record the estimator and the sample
size, plus \code{test} on a lavaan fit (the chi-square family reported)
and \code{pD} and \code{rescale} on a blavaan one (how the posterior
chi-square was rescaled).
}
\description{
\code{apa_tidy_sem_fit()} returns one row of fit indices for a fitted SEM.
For a \code{lavaan} fit those are the model chi-square with its degrees of
freedom and p value, CFI, TLI, RMSEA with its confidence interval, and
SRMR, from \code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}}. For a \code{blavaan} fit they are the
posterior predictive p value, BRMSEA and BGammaHat with their credible
intervals, from \code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}} and
\code{\link[blavaan:blavFitIndices]{blavaan::blavFitIndices()}}. The two are complementary: a fit of one
kind carries none of the other's indices, and those columns are \code{NA}.
apabayes computes no index of its own, and none of its output judges a
fit.
}
\section{Methods (by class)}{
\itemize{
\item \code{apa_tidy_sem_fit(blavaan)}: A \code{blavaan} fit. The posterior predictive
p value comes from \code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}} and BRMSEA and BGammaHat
from \code{\link[blavaan:blavFitIndices]{blavaan::blavFitIndices()}}, summarised as blavaan summarises
them: the posterior median with a highest-density interval, which is
what \code{summary()} of that object prints. A blavaan fit carries no
chi-square, CFI, TLI, RMSEA or SRMR at all, so every column of the
lavaan row is \code{NA} here, and \code{test} and \code{rmsea_level} are absent
from this method rather than accepted and ignored. BCFI, BTLI and
BNFI need a baseline model and are not reported yet. A fit made with
\code{test = "none"} has neither a PPP nor fit indices and is refused.

\item \code{apa_tidy_sem_fit(lavaan)}: A \code{lavaan} fit. \code{test = "scaled"} or
\code{"robust"} needs a fit with a scaled test statistic.

}}
\examples{
\dontshow{if (rlang::is_installed("lavaan")) withAutoprint(\{ # examplesIf}
fit <- lavaan::cfa(
  "visual =~ x1 + x2 + x3
   speed  =~ x7 + x8 + x9",
  data = lavaan::HolzingerSwineford1939
)
apa_tidy(fit, component = "loading", standardize = TRUE)
apa_tidy_sem_fit(fit, model = "Two factors")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=apa_tidy]{apa_tidy()}} for the parameter table.
}
