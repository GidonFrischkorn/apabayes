% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_num}
\alias{apa_num}
\title{Format numbers in APA style}
\usage{
apa_num(x, digits = 2, leading_zero = TRUE, big_mark = TRUE, markup = NULL)
}
\arguments{
\item{x}{Numeric vector. \code{NA}, \code{NaN}, \code{Inf} and \code{-Inf} are allowed.}

\item{digits}{Single whole number of decimals to print (fixed, never
significant digits).}

\item{leading_zero}{\code{FALSE} drops the zero before the decimal point
(\code{.47}, \code{-.47}), the APA rule for statistics that cannot exceed 1
in absolute value (correlations, standardized paths, proportions).
Values of 1 or more keep their digits.}

\item{big_mark}{\code{TRUE} separates groups of three digits with a comma
(\verb{1,240}), the APA rule for numbers of 1,000 or more.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}
}
\value{
A character vector of \code{length(x)} without names; \code{NA} in
gives \code{NA_character_} out.
}
\description{
Fixed decimals, an optional leading zero, a thousands separator and the
minus sign of the markup target. Every other formatter in apabayes
builds on this one.
}
\details{
Rounding is C \code{printf} rounding through \code{\link[=formatC]{formatC()}}, the same as
\code{papaja::apa_num()}. A result that would read \code{-0.00} is printed
\code{0.00}.
}
\section{papaja}{

papaja also exports a function called \code{apa_num()}. Whichever of the
two packages is attached last masks the other's. With both attached,
call \code{apabayes::apa_num()}. papaja's version prints a hyphen where
this one prints the minus sign.
}

\examples{
apa_num(c(0.4712, -0.4712, 1234.5678, NA))
apa_num(0.4712, leading_zero = FALSE)
apa_num(1240, digits = 0)
apa_num(c(Inf, -Inf), markup = "latex")
}
\seealso{
\code{\link[=apa_p]{apa_p()}}, \code{\link[=apa_pd]{apa_pd()}}, \code{\link[=apa_prob]{apa_prob()}}, \code{\link[=apa_ci]{apa_ci()}}, \code{\link[=apa_bf]{apa_bf()}},
\code{\link[=apa_er]{apa_er()}}, \code{\link[=apa_rhat_ess]{apa_rhat_ess()}}.
}
