% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_bf}
\alias{apa_bf}
\title{Format Bayes factors}
\usage{
apa_bf(
  x,
  direction = c("10", "01"),
  style = c("auto", "sci", "plain"),
  digits = 2,
  big_mark = TRUE,
  markup = NULL,
  symbol = FALSE
)
}
\arguments{
\item{x}{Numeric vector of Bayes factors \strong{as BF10}, evidence for H1
over H0, the scale that \code{bayestestR}, \code{brms::hypothesis()} and
\code{BayesFactor} return. Values of 0 or more; \code{NA} and \code{Inf} allowed.}

\item{direction}{\code{"10"} prints BF10 as given; \code{"01"} prints
BF01 = 1 / BF10 and the subscript \code{01}.}

\item{style}{Regime selection; see Details.}

\item{digits}{Decimals in the two-decimal regime and in the mantissa
under \code{style = "sci"}.}

\item{big_mark}{\code{TRUE} separates groups of three digits with a comma
(\verb{1,240}), the APA rule for numbers of 1,000 or more.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}

\item{symbol}{\code{TRUE} prepends \verb{*BF*~10~ = } (or \verb{*BF*~01~ = }).}
}
\value{
A character vector of \code{length(x)}; \code{NA} in gives
\code{NA_character_} out.
}
\description{
Prints a Bayes factor as a number, in the regime that keeps it
readable, with the subscript that matches the direction of the number.
No verbal category is attached; see \code{\link[=apa_bf_label]{apa_bf_label()}} for the opt-in
helper and the caveat.
}
\details{
With \code{style = "auto"}, the value after \code{direction} is applied prints
as
\itemize{
\item the infinity symbol when infinite, \code{0} when zero;
\item a mantissa to one decimal times a power of ten when 10,000 or more,
or below \code{10^-digits} (\verb{1.2 × 10^5^}, \verb{4.0 × 10^−4^}), so that a
small BF10 never prints as \code{0.00};
\item one decimal from 10 up to 10,000 (\code{20.9});
\item \code{digits} decimals otherwise (\code{5.34}).
}

\code{style = "sci"} prints every finite positive value as a mantissa with
\code{digits} decimals times a power of ten (\verb{2.09 × 10^1^}).
\code{style = "plain"} never uses scientific notation: one decimal from 10
up, \code{digits} decimals below.

The reporting guidelines this package follows treat the Bayes factor
as a continuous measure of relative evidence and ask for the number
with an unambiguous direction (Tendeiro et al., 2024; van Doorn et
al., 2021). Report the prior, the estimation method and the posterior
estimate next to it, and do not read a value near 1 as evidence of
absence.
}
\examples{
apa_bf(c(0.05, 5.3412, 20.86, 123456, Inf, NA))
apa_bf(20.86, direction = "01", symbol = TRUE)
apa_bf(20.86, style = "sci")
apa_bf(123456, markup = "latex")
}
\references{
Tendeiro, J. N., Kiers, H. A. L., Hoekstra, R., Wong, T. K., & Morey,
R. D. (2024). Diagnosing the misuse of the Bayes factor in applied
research. \emph{Advances in Methods and Practices in Psychological
Science, 7}(1). \doi{10.1177/25152459231213371}

van Doorn, J., van den Bergh, D., Böhm, U., Dablander, F., Derks, K.,
Draws, T., Etz, A., Evans, N. J., Gronau, Q. F., Haaf, J. M., Hinne,
M., Kucharský, Š., Ly, A., Marsman, M., Matzke, D., Gupta, A. R. K.
N., Sarafoglou, A., Stefan, A., Voelkel, J. G., & Wagenmakers, E.-J.
(2021). The JASP guidelines for conducting and reporting a Bayesian
analysis. \emph{Psychonomic Bulletin & Review, 28}(3), 813–826.
\doi{10.3758/s13423-020-01798-5}
}
\seealso{
\code{\link[=apa_er]{apa_er()}} for evidence ratios from \code{brms::hypothesis()},
\code{\link[=apa_bf_label]{apa_bf_label()}} for verbal categories.
}
