% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_p}
\alias{apa_p}
\alias{apa_pd}
\alias{apa_prob}
\title{Format p values, probabilities of direction and proportions}
\usage{
apa_p(x, digits = 3, markup = NULL, symbol = FALSE)

apa_pd(x, digits = 3, markup = NULL, symbol = FALSE)

apa_prob(x, digits = NULL, percent = FALSE, markup = NULL)
}
\arguments{
\item{x}{Numeric vector in [0, 1]; \code{NA} allowed.}

\item{digits}{Single whole number of decimals, at least 1. For
\code{apa_prob()} the default is 3 for proportions and 1 for percentages.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}

\item{symbol}{\code{TRUE} prepends the statistic symbol in the markup of the
target: \verb{*p* = .023}, \verb{*pd* > .999}.}

\item{percent}{\code{apa_prob()} only: print \verb{12.3\%} instead of \code{.123}. The
floor and cap apply on the percentage scale (\verb{< 0.1\%}, \verb{> 99.9\%}).}
}
\value{
A character vector of \code{length(x)}; \code{NA} in gives
\code{NA_character_} out.
}
\description{
Three decimals without a leading zero, floored at \verb{< .001} and capped
at \verb{> .999} (with \code{digits = 3}), so that a value which cannot be
distinguished from 0 or 1 at that precision is never printed as \code{.000}
or \code{1.000}. Exact 0 and exact 1 fall under the floor and the cap: a
probability of direction of 1 from a finite number of draws is
reported \verb{> .999}.
}
\details{
The probability of direction (pd) is the share of the posterior on
the side of the median's sign. It says how certain the sign of an
effect is and carries no information in favour of a null value; a pd
of \code{.500} means the sign is undetermined, not that the effect is
absent (Makowski et al., 2019). Report an interval or a ROPE share
next to it when evidence for a null is the question.
}
\examples{
apa_p(c(0.0234, 0.0004, 0.9996, NA))
apa_p(0.0234, symbol = TRUE)
apa_pd(c(0.9874, 1), symbol = TRUE)
apa_prob(0.1234)
apa_prob(0.1234, percent = TRUE)
}
\references{
Makowski, D., Ben-Shachar, M. S., Chen, S. H. A., & Lüdecke, D.
(2019). Indices of effect existence and significance in the Bayesian
framework. \emph{Frontiers in Psychology, 10}, 2767.
\doi{10.3389/fpsyg.2019.02767}
}
\seealso{
\code{\link[=apa_num]{apa_num()}} for the shared rounding, \code{\link[=apa_bf]{apa_bf()}} for evidence
in favour of a hypothesis.
}
