% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value.R
\name{apa_value}
\alias{apa_value}
\alias{apa_value.apabayes_tidy}
\alias{apa_value.default}
\title{Read one value out of a tidy table}
\usage{
apa_value(x, ...)

\method{apa_value}{apabayes_tidy}(
  x,
  term = NULL,
  rhs = NULL,
  op = NULL,
  group = NULL,
  ...,
  column = NULL
)

\method{apa_value}{default}(
  x,
  term = NULL,
  rhs = NULL,
  op = NULL,
  group = NULL,
  ...,
  column = NULL
)
}
\arguments{
\item{x}{An \link{apabayes_tidy} table, or an object \code{\link[=apa_tidy]{apa_tidy()}} accepts.}

\item{...}{Tidy method: must be empty. Default method: passed to
\code{\link[=apa_tidy]{apa_tidy()}}.}

\item{term}{The row: a term, a label, a hypothesis string, a model,
the left-hand side of a structural-equation path, or one variable of
a correlation. \code{NULL} for all rows.}

\item{rhs}{The right-hand side of a structural-equation path, or the
other variable of a correlation.}

\item{op}{The operator of a structural-equation path as lavaan writes
it: \code{"=~"}, \code{"~~"}, \code{"~"}, \code{"~1"} or \code{":="}. \code{NULL} matches any; a
correlation's is \code{"~~"}.}

\item{group}{A value of the \code{group} column to restrict the search to.}

\item{column}{The column to read, as a single string; any column of
the table, contract or not. \code{NULL} reads the column the table's type
is about — the value its contract requires beside the columns that
name a row: \code{estimate} on a parameters, hypotheses, contrasts or
correlations table, \code{elpd_diff} on a \code{loo} table, \code{bf} on a
\code{bf_models} or \code{bf_inclusion} one. A \code{sem_fit} table carries several
indices and a \code{diagnostics} table three statistics, so neither has a
default and \code{column} must name one.}
}
\value{
The selected values of \code{column}, unnamed, in the table's own
row order.
}
\description{
\code{apa_value()} addresses a row exactly as \code{\link[=apa_inline]{apa_inline()}} does and
returns the value in one of its columns: the number itself, not the
string. It is the function for a value that goes into arithmetic, into
a comparison, or into a sentence whose author formats it themselves —
the cases a hand-rolled lookup helper is otherwise written for, and
those helpers index by position and report the wrong row the day the
table changes.
}
\section{Addressing a row}{

\code{term}, \code{rhs}, \code{op} and \code{group} are \code{\link[=apa_inline]{apa_inline()}}'s, with
\code{\link[=apa_inline]{apa_inline()}}'s meaning and \code{\link[=apa_inline]{apa_inline()}}'s refusals: a term, a
label, a term with the brms class prefix removed, a hypothesis string,
a model, the two sides of a structural-equation path, a correlation's
pair in either order. No match, or more than one, is an error that
lists the candidates. \code{term = NULL} selects every row and returns the
whole column.
}

\section{What comes back}{

The column as it is stored: a double from \code{estimate}, \code{ci_low} or
\code{pd}, a character from \code{term} or \code{ci_method}, a logical from \code{std}.
Nothing is rounded, formatted or marked up, and the value carries no
names. In prose, format it — \code{apa_num(apa_value(t, "wt"))} — or use
\code{\link[=apa_inline]{apa_inline()}}, which writes the whole string. In arithmetic, use it
as it is.

A column that is \code{NA} on every addressed row is an error rather than a
missing value: a value queried for a manuscript is one the table is
expected to hold, and \code{NA} reaching the prose is the defect this
refusal exists to stop. A column missing on some rows and present on
others comes back as it is.
}

\examples{
t <- apabayes_tidy(
  data.frame(
    term = c("b_Intercept", "b_wt"), label = c("(Intercept)", "wt"),
    estimate = c(37.3, -5.34), ci_low = c(31.2, -6.85),
    ci_high = c(43.1, -3.82), pd = c(1, 0.9995),
    component = "conditional"
  ),
  type = "parameters", centrality = "median", ci_method = "eti",
  ci_level = 0.95
)
apa_value(t, "wt")
apa_value(t, "wt", column = "ci_low")
apa_value(t, column = "term")
# the number behind the string
apa_num(apa_value(t, "wt") * 2)
\dontshow{if (rlang::is_installed("lavaan")) withAutoprint(\{ # examplesIf}
fit <- lavaan::cfa("visual =~ x1 + x2 + x3", lavaan::HolzingerSwineford1939)
apa_value(fit, "visual", "x2", op = "=~", standardize = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=apa_inline]{apa_inline()}} for the formatted string, \code{\link[=apa_tidy]{apa_tidy()}} for the
tables.
}
