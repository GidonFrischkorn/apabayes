% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa-results.R
\name{apa_results}
\alias{apa_results}
\alias{is_apa_results}
\alias{print.apabayes_results}
\alias{format.apabayes_results}
\alias{as.character.apabayes_results}
\alias{knit_print.apabayes_results}
\title{Inline results}
\usage{
is_apa_results(x)

\method{print}{apabayes_results}(x, ...)

\method{format}{apabayes_results}(x, ...)

\method{as.character}{apabayes_results}(x, ...)

\method{knit_print}{apabayes_results}(x, ..., inline = FALSE)
}
\arguments{
\item{x}{An object.}

\item{...}{Ignored, or passed on by knitr.}

\item{inline}{Set by knitr: \code{TRUE} for inline code, \code{FALSE} for a
chunk.}
}
\value{
\code{is_apa_results()} returns a logical scalar; \code{format()} and
\code{as.character()} return \code{full_result}; \code{print()} returns \code{x}
invisibly.
}
\description{
Every \code{\link[=apa_inline]{apa_inline()}} method returns an \code{apabayes_results} object: the
strings for the running text next to the numbers they were made from.
It has the four elements papaja's \code{apa_results} has, and inherits that
class, so \code{papaja::apa_table()} accepts it.
}
\section{Elements}{

\describe{
\item{\code{estimate}}{character; the estimate with its interval, one
per reported row (\verb{*b* = 0.31, 95\% CrI [0.12, 0.50]}). \code{NA} for a
table kind that has no estimate.}
\item{\code{statistic}}{character; the statistics that follow it
(\verb{*pd* > .999}), or \code{NA} when the row carries none.}
\item{\code{full_result}}{character; the two joined with a comma, which
is what prints.}
\item{\code{table}}{the reported rows, as an \link{apabayes_tidy} table with
its attributes.}
\item{\code{markup}}{the markup target the strings were written for.}
}
}

\section{In a document}{

Inline code such as \code{`r apa_inline(fit, "wt")`} prints
\code{full_result} through a \code{knit_print} method; no \verb{$full_result} is
needed. \code{print()}, \code{format()} and \code{as.character()} return the same
string at the console.
}

\examples{
t <- apabayes_tidy(
  data.frame(
    term = "b_wt", estimate = -5.34, ci_low = -6.85,
    ci_high = -3.82, pd = 0.9995, component = "conditional"
  ),
  type = "parameters", centrality = "median", ci_method = "eti",
  ci_level = 0.95
)
r <- apa_inline(t, "b_wt")
r
is_apa_results(r)
r$estimate
r$statistic
}
\seealso{
\code{\link[=apa_inline]{apa_inline()}}.
}
