% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence.R
\name{apa_convergence}
\alias{apa_convergence}
\alias{apa_convergence.apabayes_tidy}
\alias{apa_convergence.default}
\title{Report convergence diagnostics in one sentence}
\usage{
apa_convergence(x, ...)

\method{apa_convergence}{apabayes_tidy}(x, ..., rhat = 1.01, ess = 400, digits = 3, markup = NULL)

\method{apa_convergence}{default}(x, ..., rhat = 1.01, ess = 400, digits = 3, markup = NULL)
}
\arguments{
\item{x}{A table of type \code{"diagnostics"} from \code{\link[=apa_tidy_diagnostics]{apa_tidy_diagnostics()}},
or any object \code{\link[=apa_tidy_diagnostics]{apa_tidy_diagnostics()}} accepts.}

\item{...}{Tidy method: must be empty. Default method: passed to
\code{\link[=apa_tidy_diagnostics]{apa_tidy_diagnostics()}} (\verb{variables =}).}

\item{rhat}{The R-hat threshold, a single number of 1 or more.}

\item{ess}{The threshold for both effective sample sizes, a single
positive number.}

\item{digits}{Decimals of R-hat.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}
}
\value{
An \link{apa_results} object whose \code{full_result} is the sentence,
with \code{estimate} \code{NA}, \code{table} the whole diagnostics table, and two
further elements: \code{passed} (logical) and \code{summary}, a one-row data
frame with the number of variables, and per diagnostic the number of
non-missing values, the extreme and the number at the threshold, the
divergence count (\code{NA} when not recorded) and both thresholds.
}
\description{
\code{apa_convergence()} summarises R-hat, the bulk and tail effective
sample sizes and the divergent transitions of every sampled quantity
into the sentence a Method or Results section quotes:
\verb{*R̂* ≤ 1.004, bulk ESS ≥ 1,240, tail ESS ≥ 980, no divergent transitions}.
}
\section{The sentence}{

Each diagnostic is stated over its non-missing values. When none
reaches its threshold the extreme is given as a bound: the largest
R-hat rounded \emph{up} to \code{digits} decimals and the smallest ESS rounded
\emph{down} to an integer, so that \verb{≤} and \verb{≥} hold for the unrounded
numbers too. When some do, the part says how many and gives the
extreme: \verb{2 of 13 *R̂* ≥ 1.01, maximum 1.018}, \verb{9 of 13 bulk ESS ≤ 400, minimum 152}. An R-hat equal to \code{rhat}, or an ESS equal to \code{ess},
counts as reaching it (Vehtari et al., 2021, recommend R-hat below
1.01 and ESS above 400).

Divergent transitions are stated as a count (\verb{no divergent transitions}, \verb{3 divergent transitions}) when the table records them,
which \code{\link[=apa_tidy_diagnostics]{apa_tidy_diagnostics()}} does for fits sampled with NUTS. Draws,
\code{mcmc.list} objects and tables stored without the record print no
divergence part, and the sentence then says nothing about divergences.

No word judging the fit is printed. The \code{passed} element is \code{TRUE}
when no part reports a value at a threshold and no divergent transition
was counted; it is for code, not for the text.
}

\examples{
t <- apabayes_tidy(
  data.frame(
    term = c("b_Intercept", "b_wt", "sigma"),
    rhat = c(1.0012, 1.0036, 1.0008),
    ess_bulk = c(1240.6, 1810, 2203), ess_tail = c(980.2, 1422, 1733)
  ),
  type = "diagnostics", centrality = NA_character_,
  ci_method = NA_character_, ci_level = NA_real_, divergences = 0L
)
apa_convergence(t)
apa_convergence(t, ess = 1000, markup = "plain")
\dontshow{if (rlang::is_installed("posterior")) withAutoprint(\{ # examplesIf}
z <- stats::qnorm(stats::ppoints(400))
draws <- posterior::as_draws_df(data.frame(
  mu = 2 + z[order(sin(seq_along(z)))],
  sigma = exp(0.3 * z[order(cos(seq_along(z)))])
))
apa_convergence(draws)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=apa_tidy_diagnostics]{apa_tidy_diagnostics()}} for the table, \code{\link[=apa_rhat_ess]{apa_rhat_ess()}} for
the diagnostics of one parameter.
}
