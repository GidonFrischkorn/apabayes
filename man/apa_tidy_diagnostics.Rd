% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-blavaan.R, R/extract-brms.R
\name{apa_tidy_diagnostics.blavaan}
\alias{apa_tidy_diagnostics.blavaan}
\alias{apa_tidy_diagnostics}
\alias{apa_tidy_diagnostics.default}
\alias{apa_tidy_diagnostics.runjags}
\title{Convergence diagnostics for every sampled quantity}
\usage{
\method{apa_tidy_diagnostics}{blavaan}(x, variables = NULL, ...)

apa_tidy_diagnostics(x, ...)

\method{apa_tidy_diagnostics}{default}(x, variables = NULL, ...)

\method{apa_tidy_diagnostics}{runjags}(x, ...)
}
\arguments{
\item{x}{A fitted model, posterior draws, or anything
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}} accepts.}

\item{variables}{Character vector of variables to report, in the order
given, or \code{NULL} for every variable that is not \emph{internal} — the same
rule \code{\link[=apa_tidy]{apa_tidy()}} uses on the draws route: names ending in \verb{__},
\code{lprior}, and names starting with \code{prior_} are dropped by default and
reported when named here.}

\item{...}{Passed to the method.}
}
\value{
An \link{apabayes_tidy} tibble of type \code{"diagnostics"} with columns
\code{term}, \code{rhat}, \code{ess_bulk} and \code{ess_tail}. Its \code{divergences}
attribute is the number of divergent post-warmup transitions summed
over chains, read from the sampler's own record, for a \code{brmsfit},
\code{stanreg}, \code{stanfit}, \code{CmdStanMCMC} or \code{blavaan} fit sampled with
NUTS; it is \code{NA} for draws, \code{mcmc.list} and runjags objects and for
fits made by optimisation, variational inference or another sampler,
which have no such record.
}
\description{
\code{apa_tidy_diagnostics()} returns R-hat and bulk and tail ESS for every
variable of a fitted model's posterior, including the group-level
deviations that a parameter table does not print. It is the table
\code{\link[=apa_convergence]{apa_convergence()}} reports from: a convergence statement has to cover
what was sampled, not what a table shows.
}
\details{
Numbers come from \code{\link[posterior:summarise_draws]{posterior::summarise_draws()}}; apabayes computes no
diagnostic of its own.
}
\section{Methods (by class)}{
\itemize{
\item \code{apa_tidy_diagnostics(blavaan)}: A \code{blavaan} fit, which
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}} cannot read (measured). The chains are
\code{blavaan::blavInspect(x, "mcmc")}, named as \code{coef()} names the
parameters (\code{visual=~x2}, \code{x1~~x1}), and the diagnostics are
\code{posterior}'s, not the \code{rhat} and \code{neff} blavaan prints. A
multi-group fit is reported: its names carry the group suffix
(\code{visual=~x2.g2}).

\item \code{apa_tidy_diagnostics(default)}: Anything
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}} accepts, which includes \code{brmsfit},
\code{stanreg}, \code{stanfit}, \code{CmdStanFit}, \code{mcmc} and \code{mcmc.list}. One
coercing method serves every supported object, as on the draws route.

\item \code{apa_tidy_diagnostics(runjags)}: A \code{runjags} object keeps its chains in
\verb{$mcmc}; \code{posterior} has no method for the object itself.

}}
\examples{
\dontshow{if (rlang::is_installed("posterior")) withAutoprint(\{ # examplesIf}
z <- stats::qnorm(stats::ppoints(400))
z <- z[order(sin(seq_along(z)))]
draws <- posterior::as_draws_df(
  data.frame(mu = 2 + z, sigma = exp(0.3 * z))
)
apa_tidy_diagnostics(draws)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=apa_tidy]{apa_tidy()}} for parameter tables.
}
