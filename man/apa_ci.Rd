% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-numbers.R
\name{apa_ci}
\alias{apa_ci}
\title{Format an interval}
\usage{
apa_ci(
  low,
  high,
  level = 0.95,
  label = "CrI",
  digits = 2,
  leading_zero = TRUE,
  big_mark = TRUE,
  markup = NULL
)
}
\arguments{
\item{low, high}{Numeric vectors of the same length (one of them may
have length 1); \code{NA} and infinite bounds allowed.}

\item{level}{Single number in (0, 1), printed as a percentage before
the label: \code{0.95} gives \verb{95\%}, \code{0.9} gives \verb{90\%}.}

\item{label}{Interval name after the level: \code{"CrI"} (equal-tailed
credible interval, the default), \code{"HDI"}, \code{"HPD"}, \code{"CI"}. \code{NULL}
drops the prefix and returns the bracket only, for table cells whose
header carries the level.}

\item{digits}{Single whole number of decimals to print (fixed, never
significant digits).}

\item{leading_zero}{\code{FALSE} drops the zero before the decimal point
(\code{.47}, \code{-.47}), the APA rule for statistics that cannot exceed 1
in absolute value (correlations, standardized paths, proportions).
Values of 1 or more keep their digits.}

\item{big_mark}{\code{TRUE} separates groups of three digits with a comma
(\verb{1,240}), the APA rule for numbers of 1,000 or more.}

\item{markup}{\code{NULL} or one of \code{"md"}, \code{"latex"}, \code{"typst"}, \code{"plain"}.
\code{NULL} uses \code{getOption("apabayes.markup")}, then the knitr output
format, then \code{"md"}. Decides the minus sign (U+2212 outside
\code{"plain"}) and the infinity symbol.}
}
\value{
A character vector; \code{NA} in either bound gives
\code{NA_character_} for that element.
}
\description{
\verb{95\% CrI [0.20, 0.74]}, with the same digits and leading-zero rule as
the estimate it accompanies and a label that names the interval type.
}
\details{
The label is part of the report: a reader must be able to tell an
equal-tailed interval from a highest-density interval, because the two
differ for skewed posteriors. apabayes never relabels; the caller
says which interval was computed.
}
\examples{
apa_ci(0.2, 0.74)
apa_ci(0.2, 0.74, leading_zero = FALSE)
apa_ci(0.2, 0.74, label = "HDI", level = 0.9)
apa_ci(-0.5, 0.74, label = NULL)
}
