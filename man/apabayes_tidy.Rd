% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy-class.R
\name{apabayes_tidy}
\alias{apabayes_tidy}
\alias{validate_apabayes_tidy}
\alias{is_apabayes_tidy}
\alias{print.apabayes_tidy}
\title{The apabayes tidy contract}
\usage{
apabayes_tidy(
  x,
  ...,
  type = c("parameters", "diagnostics", "hypotheses", "loo", "bf_models", "sem_fit",
    "contrasts"),
  centrality = c("median", "mean"),
  ci_method = "eti",
  ci_level = 0.95,
  source_class = NA_character_,
  package_versions = character()
)

validate_apabayes_tidy(x)

is_apabayes_tidy(x)

\method{print}{apabayes_tidy}(x, n = NULL, width = NULL, ...)
}
\arguments{
\item{x}{A data frame with at least the required columns of \code{type}.}

\item{...}{Further attributes, all named.}

\item{type}{Which column contract applies: one of \code{"parameters"},
\code{"diagnostics"}, \code{"hypotheses"}, \code{"loo"}, \code{"bf_models"},
\code{"sem_fit"}, \code{"contrasts"}.}

\item{centrality}{\code{"median"} or \code{"mean"}; what \code{estimate} holds. \code{NA}
when the estimate is not a posterior summary (a lavaan
maximum-likelihood estimate).}

\item{ci_method}{\code{"eti"}, \code{"hdi"}, \code{"hpd"}, \code{"spi"}, \code{"bci"},
\code{"wald"}, \code{"boot"} or \code{NA}; seeds the \code{ci_method} column when the
caller supplies none. The column describes intervals apabayes did
not necessarily compute, so it knows more methods than any route
offers through its own \code{ci} argument.}

\item{ci_level}{A number in (0, 1] or \code{NA}; seeds the \code{ci_level}
column the same way.}

\item{source_class}{Character; \code{class()} of the object the numbers
were extracted from.}

\item{package_versions}{Named character vector of the packages that
produced the numbers.}

\item{n, width}{Passed to the tibble print method.}
}
\value{
A tibble of class \code{apabayes_tidy} with the contract columns
first, in contract order, then any extra columns.
\code{validate_apabayes_tidy()} returns its input invisibly or aborts;
\code{is_apabayes_tidy()} returns \code{TRUE} or \code{FALSE}.
}
\description{
\code{apabayes_tidy()} builds the object the extract layer hands to the
format, inline and table layers: a tibble whose column names and types
are fixed by the kind of table, carrying the reporting metadata as
attributes. Every \code{apa_tidy()} method returns one, and every apabayes
function that reports numbers takes one.
}
\section{Column contracts}{

\code{type} selects the contract. Columns the caller does not supply are
filled with the typed \code{NA}, so a consumer can select a column by name
without testing whether it exists.

\code{"parameters"} — one row per model parameter. Required: \code{term},
\code{estimate}.

\describe{
\item{\code{term}}{character; the parameter as its source names it
(\code{b_wt}, \code{visual=~x1}, \code{mu}).}
\item{\code{label}}{character; the display label. Defaults to \code{term}.}
\item{\code{estimate}}{double; the posterior median or mean, per the
\code{centrality} attribute.}
\item{\code{ci_low}, \code{ci_high}}{double; interval bounds.}
\item{\code{ci_method}}{character; \code{"eti"}, \code{"hdi"}, \code{"hpd"}, \code{"spi"}
(shortest probability interval) or \code{"bci"} (bias-corrected and
accelerated) for a credible interval, \code{"wald"} or \code{"boot"}
(percentile bootstrap) for a frequentist confidence interval
(lavaan).}
\item{\code{ci_level}}{double; the interval mass, e.g. \code{0.95}.}
\item{\code{pd}}{double; probability of direction.}
\item{\code{rope_pct}}{double; percentage of the posterior inside the
ROPE, \code{NA} unless a ROPE was requested.}
\item{\code{rhat}, \code{ess_bulk}, \code{ess_tail}}{double; convergence
diagnostics.}
\item{\code{bf}}{double; a Bayes factor for the parameter, \code{NA} unless
one was requested.}
\item{\code{component}, \code{group}, \code{effects}}{character; the structure a
model puts a parameter in.}
\item{\code{std}}{logical; whether the row is a standardized estimate.}
\item{\code{p}}{double; a frequentist p value (lavaan only).}
}

The other contracts are \code{"diagnostics"} (\code{term}, \code{rhat}, \code{ess_bulk},
\code{ess_tail}), \code{"hypotheses"}, \code{"loo"}, \code{"bf_models"}, \code{"sem_fit"} and
\code{"contrasts"} (\code{contrast}, \code{group} for the \code{by} variable's value,
\code{estimate}, the interval columns, \code{pd}, \code{rope_pct}); the print method
lists the columns of each, and every extract method documents the
ones it fills. Columns beyond the contract are kept, after the
contract columns.
}

\section{Metadata}{

\code{type}, \code{centrality}, \code{ci_method}, \code{ci_level}, \code{source_class} and
\code{package_versions} are attributes of the table, plus anything passed
through \code{...} (\code{rope_range}, \code{rope_ci}, \code{bf_method}, \code{note}).
\code{ci_method} and \code{ci_level} are \emph{both} attributes and columns: a
report can mix intervals of different kinds in one table, so each row
says what it is, while the attribute says what was asked for.
}

\section{Validity}{

A tibble subclass survives \code{[} and the dplyr verbs, and nothing
re-validates on the way, so a table you have edited can carry the
class without satisfying the contract. Every apabayes function that
consumes a tidy table therefore validates it on entry;
\code{validate_apabayes_tidy()} is that check.
}

\examples{
apabayes_tidy(data.frame(term = "b_wt", estimate = -5.34))
apabayes_tidy(
  data.frame(
    term = "b_wt", estimate = -5.34, ci_low = -6.85,
    ci_high = -3.82, pd = 1
  ),
  ci_method = "hdi", source_class = "stanfit"
)
}
